from abc import ABC, abstractmethod

class Pattern(ABC): 
    # An abstract class for a 

    instructions = []

    @abstractmethod
    def __init__(self, Yarn, ):
        instructions = 


    def 
